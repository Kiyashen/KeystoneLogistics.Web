@{
    string currentController = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
    string currentAction = ViewContext.RouteData.Values["action"]?.ToString() ?? "";
    string userRole = Session["UserRole"]?.ToString() ?? "";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Keystone Logistics</title>

    <!-- Bootstrap 4.6 & FontAwesome -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css" rel="stylesheet" />
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />

    <!-- Google Fonts: Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --bg-main: #0b0f19;
            --glass-bg: rgba(255, 255, 255, 0.03);
            --glass-card: rgba(15, 23, 42, 0.85);
            --glass-border: rgba(255, 255, 255, 0.1);
            --text-main: #f8fafc;
            --text-muted: #94a3b8;
            --accent-blue: #38bdf8;
        }

        html, body {
            height: 100%;
            margin: 0;
            padding: 0;
            background-color: var(--bg-main);
            color: var(--text-main);
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            -webkit-font-smoothing: antialiased;
        }

        .main-wrapper {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            background: radial-gradient(circle at 50% 0%, rgba(30, 41, 59, 0.6) 0%, rgba(11, 15, 25, 1) 75%);
        }

        .content-body {
            flex: 1 0 auto;
            padding-top: 35px;
            padding-bottom: 60px;
        }

        /* Glassmorphism Navbar */
        .navbar-glass {
            background: rgba(11, 15, 25, 0.85);
            backdrop-filter: blur(16px);
            -webkit-backdrop-filter: blur(16px);
            border-bottom: 1px solid var(--glass-border);
            padding: 14px 28px;
        }

            .navbar-glass .navbar-brand {
                color: #ffffff !important;
                font-weight: 700;
                font-size: 1.15rem;
                letter-spacing: 0.5px;
            }

            .navbar-glass .nav-link {
                color: var(--text-muted) !important;
                font-weight: 500;
                font-size: 0.9rem;
                padding: 8px 16px !important;
                border-radius: 8px;
                transition: all 0.2s ease;
            }

                .navbar-glass .nav-link:hover {
                    color: #ffffff !important;
                    background: rgba(255, 255, 255, 0.05);
                }

            .navbar-glass .nav-item.active .nav-link {
                color: #ffffff !important;
                background: rgba(255, 255, 255, 0.08);
                border: 1px solid rgba(255, 255, 255, 0.12);
            }

        /* Card Container Styling */
        .data-card, .card {
            background: var(--glass-card) !important;
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border) !important;
            border-radius: 16px;
            box-shadow: 0 20px 50px rgba(0, 0, 0, 0.4);
            color: var(--text-main) !important;
            padding: 24px;
            overflow: hidden;
        }

        /* High-Contrast Table Overrides */
        .table {
            color: #f8fafc !important;
            margin-bottom: 0;
            background-color: transparent !important;
        }

            /* Force Header Dark Background and High Contrast Text */
            .table thead,
            .table thead tr,
            .table thead th,
            .table .thead-light th,
            .thead-light th {
                background-color: #1e293b !important;
                color: #38bdf8 !important;
                font-weight: 700 !important;
                font-size: 0.8rem !important;
                text-transform: uppercase;
                letter-spacing: 0.6px;
                border-bottom: 2px solid rgba(255, 255, 255, 0.12) !important;
                border-top: none !important;
                padding: 16px 14px !important;
            }

            .table td {
                color: #e2e8f0 !important;
                border-top: 1px solid rgba(255, 255, 255, 0.08) !important;
                vertical-align: middle;
                padding: 16px 14px;
                font-weight: 400;
            }

            /* Dark Table Text Helpers Override */
            .table text,
            .table .text-muted,
            .table small,
            .table span:not(.badge):not(.btn) {
                color: #94a3b8 !important;
            }

            .table strong,
            .table b,
            .table .text-dark {
                color: #ffffff !important;
            }

        .table-hover tbody tr:hover {
            background-color: rgba(255, 255, 255, 0.04) !important;
            color: #ffffff !important;
        }

        /* Footer */
        .footer-glass {
            background: rgba(11, 15, 25, 0.85);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-top: 1px solid var(--glass-border);
            color: var(--text-muted);
            padding: 40px 0 20px 0;
            font-size: 0.875rem;
            flex-shrink: 0;
            margin-top: auto;
        }

            .footer-glass h6 {
                color: #ffffff;
                font-weight: 600;
                margin-bottom: 16px;
                text-transform: uppercase;
                font-size: 0.75rem;
                letter-spacing: 1px;
            }

            .footer-glass ul {
                list-style: none;
                padding-left: 0;
                margin-bottom: 0;
            }

                .footer-glass ul li {
                    margin-bottom: 10px;
                }

                    .footer-glass ul li a {
                        color: var(--text-muted);
                        text-decoration: none;
                        transition: color 0.2s;
                    }

                        .footer-glass ul li a:hover {
                            color: #ffffff;
                        }

        .btn-glass-primary {
            background: rgba(56, 189, 248, 0.12);
            border: 1px solid rgba(56, 189, 248, 0.3);
            color: var(--accent-blue) !important;
            font-weight: 500;
            border-radius: 8px;
            padding: 7px 18px;
            transition: all 0.2s ease;
        }

            .btn-glass-primary:hover {
                background: rgba(56, 189, 248, 0.25);
                border-color: rgba(56, 189, 248, 0.5);
                color: #ffffff !important;
            }
    </style>
</head>
<body>
    <div class="main-wrapper">

        <!-- Glass Navbar -->
        <nav class="navbar navbar-expand-lg navbar-dark navbar-glass sticky-top">
            <div class="container">
                <a class="navbar-brand d-flex align-items-center" href="@Url.Action("Index", "Home")">
                    <i class="fa-solid fa-truck-fast text-info mr-2"></i> KEYSTONE LOGISTICS
                </a>

                <button class="navbar-toggler border-0" type="button" data-toggle="collapse" data-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>

                <div class="collapse navbar-collapse" id="navbarNav">

                    @if (Session["Username"] != null)
                    {
                        <ul class="navbar-nav mr-auto ml-lg-4">
                            <li class="nav-item @(currentController == "Home" && currentAction == "Index" ? "active" : "")">
                                <a class="nav-link" href="@Url.Action("Index", "Home")">Home</a>
                            </li>
                            <li class="nav-item @(currentController == "Home" && currentAction == "About" ? "active" : "")">
                                <a class="nav-link" href="@Url.Action("About", "Home")">About Us</a>
                            </li>
                            <li class="nav-item @(currentController == "Loads" && currentAction == "Index" ? "active" : "")">
                                <a class="nav-link" href="@Url.Action("Index", "Loads")">Freight Loads</a>
                            </li>
                            @if (userRole == "Admin")
                            {
                                <li class="nav-item @(currentController == "Customers" ? "active" : "")">
                                    <a class="nav-link" href="@Url.Action("Index", "Customers")">Customers</a>
                                </li>
                                <li class="nav-item @(currentController == "Drivers" ? "active" : "")">
                                    <a class="nav-link" href="@Url.Action("Index", "Drivers")">Drivers</a>
                                </li>
                            }
                            <li class="nav-item @(currentController == "Home" && currentAction == "Contact" ? "active" : "")">
                                <a class="nav-link" href="@Url.Action("Contact", "Home")">Contact</a>
                            </li>
                        </ul>
                    }
                    else
                    {
                        <div class="mr-auto"></div>
                    }

                    <!-- User Status / Auth Actions -->
                    <ul class="navbar-nav ml-auto align-items-center">
                        @if (Session["Username"] != null)
                        {
                            <li class="nav-item mr-3">
                                <span class="small text-muted">
                                    <i class="fa-regular fa-circle-user mr-1 text-info"></i>
                                    <strong class="text-white">@Session["Username"]</strong>
                                    <span class="badge ml-1" style="background: rgba(56,189,248,0.15); color: #38bdf8;">@Session["UserRole"]</span>
                                </span>
                            </li>
                            <li class="nav-item">
                                <a class="btn btn-outline-danger btn-sm rounded-lg" href="@Url.Action("Logout", "Account")">
                                    Logout
                                </a>
                            </li>
                        }
                        else
                        {
                            <li class="nav-item">
                                <a class="btn btn-glass-primary btn-sm" href="@Url.Action("Login", "Account")">
                                    <i class="fa-solid fa-arrow-right-to-bracket mr-1"></i> Sign In
                                </a>
                            </li>
                        }
                    </ul>
                </div>
            </div>
        </nav>

<<<<<<< HEAD
        <!-- Site-wide Alerts Partial -->
        <div class="container py-3">
            @Html.Partial("_Alerts")
        </div>

        <!-- Dynamic Body Content -->
=======
        <!-- Main Body -->
>>>>>>> origin/main
        <div class="content-body">
            @RenderBody()
        </div>

        <!-- Footer: ONLY Displays When User Is Logged In -->
        @if (Session["Username"] != null)
        {
            <footer class="footer-glass">
                <div class="container">
                    <div class="row">
                        <div class="col-md-4 mb-4">
                            <h6>Keystone Logistics</h6>
                            <p class="small text-muted mb-2">Streamlined logistics and freight management solutions.</p>
                            <p class="small text-muted mb-0"><i class="fa-solid fa-location-dot mr-1"></i> Durban, South Africa</p>
                        </div>
                        <div class="col-md-4 mb-4">
                            <h6>Quick Links</h6>
                            <ul>
                                <li><a href="@Url.Action("About", "Home")">About Company</a></li>
                                <li><a href="@Url.Action("Index", "Loads")">Loads Overview</a></li>
                                <li><a href="@Url.Action("Contact", "Home")">Support</a></li>
                            </ul>
                        </div>
                        <div class="col-md-4 mb-4">
                            <h6>Support</h6>
                            <p class="small text-muted mb-2">Need operational help?</p>
                            <a href="https://wa.me/27311234567" target="_blank" class="btn btn-sm border-0" style="background: rgba(16, 185, 129, 0.15); color: #10b981;">
                                <i class="fa-brands fa-whatsapp mr-1"></i> WhatsApp Support
                            </a>
                        </div>
                    </div>
                    <div class="text-center pt-3 mt-3 border-top" style="border-color: var(--glass-border) !important;">
                        <span class="small text-muted">&copy; @DateTime.Now.Year Keystone Logistics. All rights reserved.</span>
                    </div>
                </div>
            </footer>
        }
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("scripts", required: false)
</body>
</html>